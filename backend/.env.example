APP_NAME="Personal Finance Sheets Sync"
APP_ENV=development
DATABASE_URL=sqlite:///./finance.db

# Generate with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TOKEN_ENCRYPTION_KEY=

PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox
PLAID_PRODUCTS=transactions
PLAID_COUNTRY_CODES=US
PLAID_REDIRECT_URI=

# Shared secret sent by the frontend or Google Apps Script in X-API-Key.
APP_API_KEY=

# Optional default identity for a one-user app.
DEFAULT_USER_ID=personal
